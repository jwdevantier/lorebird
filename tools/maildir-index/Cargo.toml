[package]
name = "maildir-index"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "maildir-index"
path = "src/main.rs"

[dependencies]
loreread-core = { path = "../../crates/loreread-core" }
rusqlite = { version = "0.39", features = ["bundled"] }
clap = { version = "4", features = ["derive"] }
